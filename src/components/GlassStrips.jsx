export function GlassStrips({ layerRef }) {
  return (
    <div
      ref={layerRef}
      className="scene-two-glass-strips"
      data-testid="scene-two-glass-strips"
      aria-hidden="true"
      hidden
    />
  );
}
