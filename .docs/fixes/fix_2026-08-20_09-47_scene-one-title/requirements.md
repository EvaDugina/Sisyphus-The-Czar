# Fix Requirements — 2026-08-20 09:47

<!--
Approved snapshot of user feedback reformulated as functional and nonfunctional requirements.
Created by /fix after explicit user approval.
-->

## Паспорт

- **Дата фиксации:** 2026-08-20 09:47
- **Папка:** `.docs/fixes/fix_2026-08-20_09-47_scene-one-title/`
- **Статус:** approved

---

## Исходный текст

Пользователь сообщил: «На сцене 1 пропала надпись minioature The path of tzarey!». В проектной документации канонический двухстрочный текст зафиксирован как `miniature` и `The Path of Tzarey`.

---

## Функциональные требования

- **Заголовок сцены 1:** Система должна отображать на странице `/scene-1` надписи `miniature` и `The Path of Tzarey` в существующем оформлении и положении.
  - **Пояснение:** Разделение приложения на отдельные страницы сцен не должно удалять заголовок первой сцены.
  - **Источник:** «На сцене 1 пропала надпись minioature The path of tzarey!»
  - **Acceptance hint:** На `/scene-1` обе строки существуют в DOM, видимы пользователю и имеют ненулевой bounding box.

- **Изоляция сценических элементов:** Система должна вернуть заголовок, не показывая на сцене 1 след камня, рейтинг, таймер или другие элементы вершины, которые не относятся к первой сцене.
  - **Пояснение:** Исправление должно быть ограничено заголовком и не отменять разделение сцен.
  - **Источник:** Утверждённый черновик требования после сообщения о пропавшей надписи.
  - **Acceptance hint:** На `/scene-1` заголовок видим, а таймер, рейтинг, отпечаток камня и след остаются скрытыми.

---

## Нефункциональные требования

- **Защита от регрессии:** Smoke-тест сцены 1 должен проверять видимость обеих строк заголовка.
  - **Пояснение:** Повторное скрытие контейнера заголовка должно обнаруживаться автоматическим тестом.
  - **Источник:** Утверждённый черновик требования.
  - **Acceptance hint:** Тест падает, если `miniature` или `The Path of Tzarey` скрыты на `/scene-1`.

- **Сохранение поведения:** Логика завершения сцены 1 и внешний вид сцен 2–3 не должны измениться.
  - **Пояснение:** Исправление является локальным визуальным регрессионным fix.
  - **Источник:** Утверждённый черновик требования.
  - **Acceptance hint:** Существующие unit/integration и scene smoke-тесты проходят без изменения ожидаемой логики сцен.

---

## Проблемы и замечания, превращённые в требования

- **Контейнер заголовка скрыт целиком:** Сейчас CSS-правило для сцены 1 скрывает `.summit` вместе с содержащимися в нём заголовками. Требуемое поведение: заголовок виден, а не относящиеся к сцене 1 элементы остаются скрытыми.
  - **Источник:** «На сцене 1 пропала надпись minioature The path of tzarey!»
  - **Acceptance hint:** Вычисленное CSS-свойство `display` контейнера заголовка на `/scene-1` не равно `none`; отдельные сценические элементы по-прежнему скрыты.

---

## Открытые вопросы

- Открытых вопросов нет. Каноническое написание строк уже определено проектной документацией.

---

## Не включено / требует уточнения

- Изменение текста, шрифта, размера или расположения заголовка не включено: требуется вернуть существующий вариант.
